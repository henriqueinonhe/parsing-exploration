import { TokenString } from "../src/TokenString";

describe("constructor", () =>
{
  describe("Post Conditions", () =>
  {
    test("Lexing is performed corretly", () =>
    {
      // expect(new TokenString("P and Q").toString()).toBe("P and Q");
      // expect(new TokenString("Forall x Exists y ").toString()).toBe("Forall x Exists y ");

    });
  });
});